{
  "name": "adonis-odm-docs",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro check && astro build && npx pagefind --site dist",
    "build:astro": "astro check && astro build",
    "build:search": "npx pagefind --site dist",
    "build:cf": "astro check && astro build && npx pagefind --site dist",
    "preview": "astro preview",
    "preview:cf": "wrangler pages dev dist",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^5.8.1",
    "@astrojs/tailwind": "^5.1.2",
    "@astrojs/mdx": "^4.0.0",
    "tailwindcss": "^3.4.15",
    "@tailwindcss/typography": "^0.5.15",
    "shiki": "^1.22.2",
    "pagefind": "^1.1.1"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "typescript": "^5.6.3"
  }
}
